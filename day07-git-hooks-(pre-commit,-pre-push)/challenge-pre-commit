#!/usr/bin/env bash
# Challenge version: block direct commits to main
branch=$(git rev-parse --abbrev-ref HEAD)
if [ "$branch" = "main" ] || [ "$branch" = "master" ]; then
  echo "[ERROR] Direct commits to $branch are not allowed. Use a feature branch."
  exit 1
fi
